name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch: {}

jobs:
  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'macos-latest'
            args: '--target universal-apple-darwin'
            targets: 'aarch64-apple-darwin,x86_64-apple-darwin'
          - platform: 'windows-latest'
            args: ''
            targets: ''
          - platform: 'ubuntu-22.04'
            args: ''
            targets: ''

    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.targets }}

      - name: Install Linux build dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Install npm dependencies
        run: npm ci

      - name: Build Tauri Application
        uses: tauri-apps/tauri-action@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'Wildcat Studio ${{ github.ref_name }}'
          releaseBody: 'See release assets below for macOS (Universal - Intel & Apple Silicon), Windows, and Linux.'
          releaseDraft: false
          prerelease: false
          args: ${{ matrix.args }}

  notify-store:
    name: Notify ArmsTrader Store
    needs: [release]
    runs-on: ubuntu-latest
    steps:
      - name: Trigger ArmsTrader Store Sync
        run: |
          echo "Wildcat Studio release successfully published to GitHub."
          if [ -n "${{ secrets.ARMSTRADER_DISPATCH_TOKEN }}" ]; then
            echo "Dispatching release sync to cook0001/armstrader.store..."
            curl -s -X POST \
              -H "Accept: application/vnd.github+json" \
              -H "Authorization: Bearer ${{ secrets.ARMSTRADER_DISPATCH_TOKEN }}" \
              https://api.github.com/repos/cook0001/armstrader.store/dispatches \
              -d '{"event_type": "app_released", "client_payload": {"app": "wildcat-studio", "version": "${{ github.ref_name }}"}}' || true
          else
            echo "Notice: ARMSTRADER_DISPATCH_TOKEN not configured in source repository."
            echo "ArmsTrader Store autonomous 2-hour cron sync will detect and stream these new binaries to the server."
          fi

